{
  "name": "fiesta",
  "version": "0.0.1",
  "description": "Landing page",
  "license": "No license",
  "author": "https://github.com/marvincayetano",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "parcel-bundler": "^1.12.4",
    "postcss": "^8.3.5",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.2.4"
  },
  "dependencies": {
    "components": "^0.1.0",
    "tailwind": "^4.0.0",
    "utilities": "^1.0.5"
  }
}
